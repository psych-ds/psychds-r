"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validate = void 0;
exports.run = run;
const dntShim = __importStar(require("../_dnt.shims.js"));
const validate_js_1 = require("./validate.js");
Object.defineProperty(exports, "validate", { enumerable: true, get: function () { return validate_js_1.validate; } });
const output_js_1 = require("./utils/output.js");
const options_js_1 = require("./setup/options.js");
const platform_js_1 = require("./utils/platform.js");
const deno_js_1 = require("./files/deno.js");
const validationProgressTracker_js_1 = require("./utils/validationProgressTracker.js");
const node_process_1 = __importDefault(require("node:process"));
/**
 * Main function to run the validator in a Node.js environment.
 * This function is designed to work with both ESM and CJS in npm.
 *
 * @param {string[]} args - Command line arguments.
 */
async function run(args = []) {
    try {
        await (0, platform_js_1.initializePlatform)();
        // Parse command line options
        const options = (0, options_js_1.parseOptions)(args);
        // Convert relative path to absolute
        const absolutePath = platform_js_1.path.resolve(options.datasetPath);
        // Read the file tree from the specified path
        const fileTree = await (0, deno_js_1.readFileTree)(absolutePath);
        if (options.useEvents) {
            // Create event emitter
            const emitter = new platform_js_1.EventEmitter();
            // Start progress tracker
            const progressTracker = new validationProgressTracker_js_1.ValidationProgressTracker(emitter);
            // Validate
            const _resultPromise = await (0, validate_js_1.validate)(fileTree, {
                ...options,
                useEvents: true,
                emitter,
            });
            await progressTracker.waitForCompletion();
            // Use a platform-agnostic way to exit
            if (typeof node_process_1.default !== "undefined" && node_process_1.default.exit) {
                node_process_1.default.exit(0);
            }
            else if (typeof dntShim.Deno !== "undefined") {
                dntShim.Deno.exit(0);
            }
        }
        // Perform validation
        const result = await (0, validate_js_1.validate)(fileTree, options);
        // Output results based on specified format
        if (options.json) {
            // Convert Map to Array for JSON serialization
            console.log(JSON.stringify(result, (_, value) => value instanceof Map ? Array.from(value.values()) : value));
        }
        else {
            // Format output for console
            console.log(await (0, output_js_1.consoleFormat)(result, {
                verbose: options.verbose ?? false,
                showWarnings: options.showWarnings ?? false,
            }));
        }
    }
    catch (error) {
        console.error("An error occurred:", error);
    }
}
// Run the validator if this is the main module
if (typeof dntShim.Deno !== "undefined" && dntShim.Deno.mainModule === require("url").pathToFileURL(__filename).href) {
    run(dntShim.Deno.args);
}
else if (typeof node_process_1.default !== "undefined" && node_process_1.default.argv[1] === require("url").pathToFileURL(__filename).href) {
    run(node_process_1.default.argv.slice(2));
}
