"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.readElements = void 0;
exports._readElements = _readElements;
const memoize_js_1 = require("../utils/memoize.js");
//split filename into constituent parts
function _readElements(filename) {
    let extension = "";
    let suffix = "";
    const keywords = {};
    const parts = filename.split("_");
    for (let i = 0; i < parts.length - 1; i++) {
        const [key, value] = parts[i].split("-");
        keywords[key] = value || "NOKEYWORD";
    }
    const lastPart = parts[parts.length - 1];
    const extStart = lastPart.indexOf(".");
    if (extStart === -1) {
        suffix = lastPart;
    }
    else {
        suffix = lastPart.slice(0, extStart);
        extension = lastPart.slice(extStart);
    }
    return { keywords, suffix, extension };
}
exports.readElements = (0, memoize_js_1.memoize)(_readElements);
