% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_psych_ds_app}
\alias{run_psych_ds_app}
\title{Run the Psych-DS Shiny Application}
\usage{
run_psych_ds_app(
  launch.browser = getOption("shiny.launch.browser", interactive()),
  port = getOption("shiny.port"),
  host = getOption("shiny.host", "127.0.0.1"),
  startup_mode = getOption("psychds.startup_mode", "recommended"),
  force_browser = getOption("psychds.force_browser", FALSE),
  ...
)
}
\arguments{
\item{launch.browser}{Logical or function. Should the app be opened in a
browser? Defaults to \code{TRUE} in interactive sessions.}

\item{port}{Integer. Port number for the application. If \code{NULL}, a random
available port is used.}

\item{host}{Character. Host IP address. Defaults to \code{"127.0.0.1"} (localhost).}

\item{startup_mode}{Character. One of \code{"strict"}, \code{"recommended"}, or
\code{"minimal"}. Controls dependency checking strictness:
\itemize{
\item \code{"strict"}: Requires all packages at minimum versions
\item \code{"recommended"}: Warns about outdated packages but continues
\item \code{"minimal"}: Only checks for critical dependencies
}}

\item{force_browser}{Logical. Force opening in external browser. Recommended
for RStudio users experiencing viewer issues.}

\item{...}{Additional arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
This function does not return a value. It launches the Shiny
application and blocks until the app is closed.
}
\description{
Launches the interactive Shiny application for creating, editing, and
validating Psych-DS compliant datasets.
}
\details{
The application provides a guided interface for:
\itemize{
\item Selecting project directories and data files
\item Adding dataset metadata
\item Standardizing filenames with Psych-DS keywords
\item Generating data dictionaries
\item Validating datasets against the Psych-DS specification
\item Uploading to the Open Science Framework (OSF)
}
}
\examples{
\dontrun{
# Standard launch
run_psych_ds_app()

# Force external browser (if RStudio viewer has issues)
run_psych_ds_app(force_browser = TRUE)

# Minimal checking for faster startup
run_psych_ds_app(startup_mode = "minimal")

# Specific port
run_psych_ds_app(port = 3838)
}

}
\seealso{
\code{\link[=check_psychds_deps]{check_psychds_deps()}} for verifying dependencies before running.
}
